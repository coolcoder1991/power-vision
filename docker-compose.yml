version: '3.1'
services:
    db:
        image: postgres:latest
        restart: always
        ports:
            - 5432:5432
        container_name: dg_postgres
        environment:
            POSTGRES_USER: admin
            POSTGRES_PASSWORD: admin
            POSTGRES_DB: batter
